name: Build

runs:
  using: "composite"
  steps:
    - run: |
        cd web
        dotnet restore
      shell: bash
    - run: |
        ls \
        cd web \
        dotnet build
      shell: bash
    - run: |
        ls \
        cd web \
        dotnet test
      shell: bash
