name: Build

runs:
  using: "composite"
  steps:
    - run: |
        cd web
        dotnet restore
      shell: bash
    - run: |
        cd web
        dotnet build --no-restore
      shell: bash
    - run: |
        cd web
        dotnet test --no-restore --verbosity normal
      shell: bash
