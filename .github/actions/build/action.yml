name: Build

runs:
  using: "composite"
  steps:
    - run: |
        cd web
        dotnet restore
      shell: bash
    - run: dotnet build
      shell: bash
    - run: dotnet test
      shell: bash
